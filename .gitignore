# 本机 Agent / IDE / 临时资源 / 本地入口（精确忽略条目，来自 AI Workspace 规范）
.idea/
.vscode/
.local/
BOOTSTRAP.md
.claude/
.codex/
.config/opencode/
.agents/

# 标准构建产物与依赖（精确、非通配，保证仓库可提交源码而非产物）
web/node_modules/
web/dist/
bin/
coverage.out

# 本地运行时配置（含可能写密钥；模板 .env.example / config.example.yaml 已提交）
.env
config.yaml
web/components.d.ts
